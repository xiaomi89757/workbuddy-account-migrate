# WorkBuddy 换账号「无感迁移」傻瓜式操作手册（V2）

> **这份手册可以独立使用**：即使你关闭了 WorkBuddy 对话、甚至重装了客户端，只要照着下面做，旧账号里的【对话记录、长期记忆、定时任务（自动化）、连接器】都能原样带到新账号，一个都不丢。
>
> **核心原则（务必记住）**：换账号不会删除任何数据，所有东西都在你电脑本地硬盘上。本工具只做「改归属 + 合并」，绝不删除原始文件。旧账号残留的记忆文件、连接器目录都会保留，可随时手动还原。

---

## 一、先看懂（30 秒）

- **换账号不会删除任何数据**，数据都在本地硬盘 `C:\Users\123\.workbuddy\` 里。
- 但 WorkBuddy 按账号「隔离」数据：换到新号后，旧号的对话 / 记忆 / 定时任务会「看不见」。
- 本手册教你怎么**一键把它们过户到新号**，做到无感切换。
- 当前 WorkBuddy 版本里，**定时任务是绑定账号的**（带 `owner_user_id`），所以光换号不会自动带过去，必须跑一次本脚本过户——这是和旧版"全局共享"说法不一样的地方。

**已经帮你准备好的东西（不用自己弄）：**
| 项目 | 位置 |
|------|------|
| 一键迁移脚本（V2，非破坏式） | `C:\Users\123\WorkBuddy\2026-09-04-19-51-50\migrate_account.py` |
| 更早的整目录安全备份 | `C:\Users\123\.workbuddy_backup_20260904_202201` |
| 定时任务配置备份 | `C:\Users\123\WorkBuddy\2026-09-04-19-51-50\automation_backup_20260904.md` |

> 脚本每次迁移前会自动再备份一份到 `C:\Users\123\.workbuddy\migrate_backups\<时间戳>\`，双重保险。

---

## 二、正式操作（共 4 步，照做即可）

### 第 1 步：退出旧账号，登录新账号
1. 打开 WorkBuddy。
2. 点左下角（或右上角）的**头像 / 账号名**。
3. 在弹出的菜单里找「**设置**」或「**退出登录** / **切换账号**」，点它。
4. 退出后，用你的**另一个 WorkBuddy 账号**重新登录。
5. 登录成功后，随便发一句话或新建一个任务，确认新账号能正常使用。

### 第 2 步：打开命令行窗口
- **推荐 Git Bash**：桌面上找「Git Bash」图标双击打开；没有的话，开始菜单搜「Git Bash」。
- **或者 PowerShell**：按 `Win + R` 键，输入 `powershell`，回车。

### 第 3 步：先看预览（强烈建议，只读不改）
在打开的窗口里，**整行复制下面命令，右键粘贴，按回车**：

- **Git Bash** 用这个：
```
C:/Users/123/.workbuddy/binaries/python/versions/3.13.12/python.exe C:/Users/123/WorkBuddy/2026-09-04-19-51-50/migrate_account.py --dry-run
```
- **PowerShell** 用这个：
```
& "C:\Users\123\.workbuddy\binaries\python\versions\3.13.12\python.exe" "C:\Users\123\WorkBuddy\2026-09-04-19-51-50\migrate_account.py" --dry-run
```

脚本会列出你电脑上的所有账号，并显示「将迁移什么」（对话/定时任务/待投递条数），**不会改动任何数据**。确认列表里的「目标=新账号」和「源=旧账号」符合要求，再进入第 4 步正式迁移。

### 第 4 步：正式迁移（交互选新旧账号）
把上面命令里的 `--dry-run` **去掉**，重新运行：

- **Git Bash**：
```
C:/Users/123/.workbuddy/binaries/python/versions/3.13.12/python.exe C:/Users/123/WorkBuddy/2026-09-04-19-51-50/migrate_account.py
```
- **PowerShell**：
```
& "C:\Users\123\.workbuddy\binaries\python\versions\3.13.12\python.exe" "C:\Users\123\WorkBuddy\2026-09-04-19-51-50\migrate_account.py"
```

脚本会先自动备份，再列出账号（带 `可能当前<--` 提示），然后问你两个问题，**照着答、回车**：
- 问「请输入【目标=新账号】的序号」→ 输入**新账号**前面的数字（列表里带 `可能当前` 提示的通常就是它）。
- 问「要合并的旧账号序号」→ 输入**旧账号**的数字（例如 `1,3`），或输入 `all` 把全部旧账号合并过来。

跑完会显示「✅通过 / 全部完成」，并提示重启客户端。

### 第 5 步：重启 WorkBuddy
完全退出 WorkBuddy（关闭所有窗口 / 右键托盘退出），再重新打开并登录新账号。
等几秒缓存刷新后，你就能在新账号里看到旧账号的**全部对话、记忆、定时任务（自动化列表里显示 ACTIVE）、连接器**了。

---

## 三、怎么确认成功了？
- 打开「自动化 / 定时任务」列表，能看到原来的任务（如每日积分自动领取、唐山松汀公众号每日同步），且状态是 **ACTIVE**。
- 历史对话列表里能看到以前的聊天记录。
- 记忆 / 长期记忆正常显示。

---

## 四、关于「旧账号残留文件」——放心，保留就好

迁移后，`C:\Users\123\.workbuddy\memory\` 下仍可能留着旧账号的记忆文件（如 `b5255a92_*.md`、`f41130e6_*.md`），`connectors\` 下也留着旧账号子目录。**这些是正常的、无害的**：
- 它们只是「死文件」，客户端按当前账号读取，不影响使用；
- 保留它们等于多一份可还原副本，**符合"数据零丢失"原则，不要手动删除**；
- 只有当你确认永远不再用旧账号、且已验证新账号数据完整时，才可考虑清理（清理前务必先确认 `migrate_backups` 备份在）。

---

## 五、万一出问题（兜底，别慌）

**情况 A：第 4 步列出的账号里找不到旧账号（旧数据消失了）**
1. **不要**自己乱改任何文件。
2. 备份在 `C:\Users\123\.workbuddy_backup_20260904_202201` 和 `C:\Users\123\.workbuddy\migrate_backups\`，用它们可恢复。
3. 联系协助处理，说明情况即可。

**情况 B：想反悔 / 回滚到迁移前**
运行（把 `latest` 换成具体时间戳也可）：
```
C:/Users/123/.workbuddy/binaries/python/versions/3.13.12/python.exe C:/Users/123/WorkBuddy/2026-09-04-19-51-50/migrate_account.py --rollback latest
```
脚本会先对「当前状态」再做一次安全快照，再恢复到最近一次迁移前备份，保证可再回滚。

**情况 C：运行报错**
1. 把报错文字截图或复制下来。
2. 先检查命令是否**整行**完整粘贴（别漏了路径）。
3. 仍不行就用上面的备份恢复，并寻求帮助，不要反复乱跑。

---

## 六、云端同步提示（如果你开过 WorkBuddy 云端同步）

本机存在 `edge-sync-mapping-*.db`（云端同步索引）。如果你之前开启过云端同步：
- 换号后重新登录，会触发一次**云端身份切换 + 同步**；
- 本地迁移的结果是「本地过户」，以本地磁盘数据为准，不受云端覆盖影响；
- 若发现云端把新账号的旧视图同步下来、盖住了本地数据，**别急**，用 `--rollback latest` 从本地备份恢复即可，并以本地为准。

---

## 七、常见问题

**Q：换号后旧对话真的不会丢吗？**
A：不会。数据在你的电脑磁盘上，只是换了"查看身份"。本脚本帮你把身份切过去，原文件一律保留。

**Q：我做的项目文件（网站 / 表格 / 代码）会丢吗？**
A：不会。项目文件存在磁盘目录里，跟账号无关，换号后照常存在。

**Q：迁移需要联网吗？**
A：不需要。脚本完全在本地运行。

**Q：能反悔吗？**
A：能。脚本迁移前自动备份，且提供 `--rollback latest` 一键还原；源数据文件也保留。

**Q：定时任务换号后会自己继续跑吗？**
A：过户到新账号并重启后，原来的定时任务（ACTIVE）会照常按原时间表运行。

**Q：可以只迁移某一个旧账号吗？**
A：可以。第 4 步问"旧账号序号"时输入对应数字（如 `1`），多个用逗号 `1,3`，或 `all` 全部。

---

> 备份位置速查：迁移脚本 `…\2026-09-04-19-51-50\migrate_account.py` ｜ 自动备份 `C:\Users\123\.workbuddy\migrate_backups\` ｜ 整目录备份 `C:\Users\123\.workbuddy_backup_20260904_202201`
